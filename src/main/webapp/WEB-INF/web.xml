<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>VG Tech RH System</display-name>
    <description>Sistema de Gestión de RH — VG Tech</description>

    <!-- Sesión de 30 minutos -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <!-- ======================== SERVLET: LoginServlet ======================== -->
    <servlet>
        <servlet-name>LoginServlet</servlet-name>
        <servlet-class>com.realestate.controllers.LoginServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>LoginServlet</servlet-name>
        <url-pattern>/login</url-pattern>
    </servlet-mapping>

    <!-- ====================== SERVLET: EmpleadoServlet ====================== -->
    <!--
        NOTA: EmpleadoServlet usa @MultipartConfig para soportar el campo de foto
        (tipo file). El enctype="multipart/form-data" en el formulario HTML es
        obligatorio para que getPart("foto") funcione en Tomcat.

        FUTURO JDBC: Ver EmpleadoService.java — el INSERT a SQL Server ya está
        preparado y comentado, listo para activarse cuando se configure DatabaseConfig.
    -->
    <servlet>
        <servlet-name>EmpleadoServlet</servlet-name>
        <servlet-class>com.realestate.controllers.EmpleadoServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>EmpleadoServlet</servlet-name>
        <url-pattern>/empleados</url-pattern>
    </servlet-mapping>

    <!-- Página de bienvenida → login -->
    <welcome-file-list>
        <welcome-file>login.html</welcome-file>
    </welcome-file-list>

    <!-- Error 403 → regresar al login -->
    <error-page>
        <error-code>403</error-code>
        <location>/login.html</location>
    </error-page>

</web-app>
